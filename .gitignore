# ──────────────────────────────────────────────────────────────
# OS / editor noise
# ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.tmp
~$*
.obsidian/

# ──────────────────────────────────────────────────────────────
# Build outputs (generated artifacts; source stays tracked)
# ──────────────────────────────────────────────────────────────
/build/output/

# ══════════════════════════════════════════════════════════════
# PRIVATE / LOCAL-ONLY  —  NEVER PUBLISH TO GIT
# ──────────────────────────────────────────────────────────────
# These trees hold the supporting context & guardrails: tasker
# summaries and anything naming PFTU 26-3 / TASKORD / the authority
# chain / personnel. They exist ONLY on the local working machine to
# give the model context. Only what we PRODUCE (handbook/, deliverables/,
# build/ outputs) and a sanitized README are ever pushed.
#
# If you add a new sensitive file, put it under one of these paths.
# ══════════════════════════════════════════════════════════════
/CLAUDE.md
/context/
/decisions/
/resources/
